# .env.claude - Plaintext environment variables for claude-sandbox
#
# This file contains NON-SENSITIVE project configuration.
# Safe to commit to git.
#
# For sensitive secrets (API keys, passwords), use .env.sops instead.
#
# Usage:
# 1. Copy this to your project root as .env.claude
# 2. Update values for your project
# 3. Commit to git
#
# When claude-sandbox runs, these variables are automatically loaded.

# ============================================================================
# Database Configuration
# ============================================================================
DATABASE_NAME=myapp_development
# DATABASE_URL is often constructed from other vars, but can be set explicitly:
# DATABASE_URL=postgres://user:password@localhost:5432/myapp_dev

# ============================================================================
# Application Settings
# ============================================================================
RAILS_ENV=development
NODE_ENV=development
APP_HOST=localhost:3000
APP_PORT=3000

# ============================================================================
# Redis Configuration
# ============================================================================
REDIS_URL=redis://localhost:6379/0

# ============================================================================
# Feature Flags
# ============================================================================
ENABLE_FEATURE_X=true
ENABLE_EXPERIMENTAL_FEATURES=false

# ============================================================================
# Logging & Debugging
# ============================================================================
LOG_LEVEL=info
DEBUG=false

# ============================================================================
# Third-party Services (non-sensitive config)
# ============================================================================
# S3 bucket name (not credentials)
S3_BUCKET_NAME=myapp-uploads-dev

# Stripe mode (use .env.sops for actual keys)
STRIPE_MODE=test

# Sendgrid settings (use .env.sops for API key)
SENDGRID_FROM_EMAIL=noreply@example.com

# ============================================================================
# Build & Deployment
# ============================================================================
BUILD_ENV=development
ASSET_HOST=https://cdn.example.com

# ============================================================================
# Notes
# ============================================================================
# - This file is loaded BEFORE .env.sops
# - .env.sops values can override these if needed
# - K8s secrets are loaded first, then .env.claude, then .env.sops
# - Variables set in k8s job template take highest precedence
#
# For sensitive values, create .env.sops:
#   sops .env.sops
# See docs/SOPS-SETUP.md for details.
